# S10 巡逻赛题工程总文档（2026-08-06）

> 本文档为当前唯一工程文档，整合自 2026-08-04 版本（已归档至
> `DR_competition/_archive_20260806/0804.md`）与 8/5–8/6 的全部实验结论；
> 参数唯一来源为 `doc/s10_mpc_deploy.yaml` 与代码内默认值，本文档描述当前
> 生产状态。
> 规则来源：`doc/比赛规则_赛道四_具身未来.md`（官方 PDF OCR）+ README；
> 冲突以官方 PDF 为准。

## 1. 赛程与规则

| 节点 | 时间 | 提交物/事项 |
|---|---|---|
| 初赛 | **8.20 截止** | 技术方案 PDF + 可运行 Demo + 开源协议 + GitHub 链接 |
| 复赛 | 8.25–9.20 | 真机调试、Demo 评测、开源规范核验 |
| 决赛 | 9.22–9.23 | 线下路演/现场任务，9.23 颁奖 |

- **计分**：总成绩 = 完成时间（秒）÷ 模式系数，得分越低排名越靠前；
  30 个定位点须全部完成，未完成不计排名。
- **模式系数**（官方 PDF）：遥控 ÷1.0 / 自主跟随 ÷1.3 / 自主导航 ÷1.4。
  **已申报：自主跟随（÷1.3）**，不强制全局 A\*。
- **判点（以 mujoco 环境为准）**：`track_overlay` 共 33 航点
  （000_start~032_end），base 进入 wp0 的 0.2m 水平半径计时开始、逐点推进、
  终点停表。必须逐点全部通过、不可跳过。
- **合规**：力矩天然 clip 到 ctrlrange（腿 ±50Nm / 轮 ±14Nm）；
  硬件改装上限 = 1× AGX Orin + 1× 深度相机；仿真主机算力无限制。

## 2. 目录结构

```
DR_competition/
├── .venv/                       # 项目虚拟环境（Python 3.12.3，WSL Ubuntu）
├── dial-mpc/                    # dial-mpc 采样 MPC 库（已打 S10 补丁）
│   └── dial_mpc/
│       ├── core/dial_core.py    # MBDPI 核心（ctx 参数化 + sigma_dim + 奖励 clip）
│       └── envs/s10_env.py      # S10 轮足环境（rollout 模型 s10_mpc.xml）
├── deeprobot_competition/       # ROS2 工作空间（比赛环境）
│   ├── install/                 # colcon 构建产物（运行时需 source）
│   ├── src/S10_sdk_deploy/      # 主包：仿真节点/感知/控制器/模型
│   └── doc/                     # 本文档 + s10_mpc_deploy.yaml + 官方材料
├── refs/                        # 参考仓库（go2w_rl_gym、unitree_mujoco）
└── _archive_20260806/           # 8/6 整理归档（旧文档/旧 tmp/旧构建产物）
```

`_archive_20260806/` 内容（均可恢复）：

| 归档项 | 原位置 | 说明 |
|---|---|---|
| `0804.md` | `doc/` | 旧版工程文档（已由本文件取代） |
| `tmp_root_old_bench/` | 根目录 `tmp/` | 8/3–8/4 早期基准脚本与结果 |
| `tmp_competition_experiments/` | `deeprobot_competition/tmp/` | 8/3–8/6 实验链脚本/日志（gitignore） |
| `dial-mpc-orig/` | 根目录 | dial-mpc 上游原始克隆（git 历史可恢复，无需保留副本） |
| `build/`、`log/` | 工作空间 | colcon 构建产物（可重新生成） |
| `config_README_old.md` | `src/.../config/README.md` | 旧 LiDAR 集成说明（含过时 ROS humble/taichi 描述） |
| `MUJOCO_LOG.TXT`、`]*range=[^` | 根目录 | mujoco 日志与 0 字节杂散文件 |

## 3. 环境配置

### 3.1 系统基线

- **WSL2 + Ubuntu 24.04**（当前机器实测环境；`wsl -d Ubuntu-24.04`）
- **ROS 2 Jazzy**（`/opt/ros/jazzy`，README 要求）
- **GPU**：NVIDIA RTX 4090 24GB（驱动 591.86，`nvidia-smi` 可见）
- **Python**：3.12.3（系统 `/usr/bin/python3`）

### 3.2 虚拟环境（.venv，已就绪）

虚拟环境在仓库根目录 `DR_competition/.venv`（注意：**不在** `deeprobot_competition`
内），创建命令：

```bash
cd ~/DR_competition
/usr/bin/python3 -m venv .venv
```

关键包（实测版本）：

| 包 | 版本 | 用途 |
|---|---|---|
| jax / jaxlib / jax-cuda12-* | 0.4.38 | MBDPI 采样 MPC（**GPU 后端，实测可用**） |
| mujoco / mujoco-mjx | 3.11.0 | 物理仿真 / rollout |
| mujoco-lidar | 0.3.3 | LiDAR 仿真（Airy-96 角表） |
| numpy / scipy | 2.5.1 / 1.14.1 | 感知与数值 |
| PyYAML | 6.0.3 | 配置加载 |
| torch / brax | 2.7.0 / 0.14.2 | 参考/备用（当前主链路不用） |
| taichi | 1.7.4 | LiDAR 可选 GPU 后端（WSL 下默认不用，见下） |
| opencv-python / matplotlib / pynput | — | 工具与交互 |

安装/补充依赖：

```bash
cd ~/DR_competition
./.venv/bin/pip install "numpy<2" mujoco mujoco-lidar jax[cuda12]==0.4.38
```

### 3.3 WSL GPU 与 JAX

- `jax.default_backend()` 实测为 `gpu`（CudaDevice id=0）。
- **编译缓存**：启动前统一设置
  `export JAX_COMPILATION_CACHE_DIR=$HOME/.cache/s10_dial_mpc`
  （mpc_controller 内部兜底；缓存后冷启动 30s → ~4.3s）。
- **LiDAR 后端**：WSL 下 taichi GPU 后端可能因缺 `libcuda.so` core dump，
  **生产默认 `S10_LIDAR_BACKEND=cpu`**（实测 48 线 45.9ms/帧，满足 10Hz）。

### 3.4 ROS2 工作空间

```bash
cd ~/DR_competition/deeprobot_competition
source /opt/ros/jazzy/setup.bash
colcon build --packages-up-to s10_sdk_deploy --cmake-args -DBUILD_PLATFORM=x86
source install/setup.bash
```

- 真机目标用 `-DBUILD_PLATFORM=arm`。
- 已构建产物在 `install/`（当前可用）；`build/`/`log/` 已归档，随时可重建。
- 已安装包：`s10_sdk_deploy`、`drdds`（消息定义）。
- 运行仿真前 `source install/setup.bash`（节点 import `drdds.msg` 等消息）。

### 3.5 关键运行方式

```bash
cd ~/DR_competition/deeprobot_competition
export ROS_DOMAIN_ID=1
source /opt/ros/jazzy/setup.bash
source install/setup.bash
export JAX_COMPILATION_CACHE_DIR=$HOME/.cache/s10_dial_mpc

# 模式 B 遥控（交互）：S10_MPC_ENABLE=1，窗口内 z 站起 / c 进入 MPC / wasd 移动 / qe 转向
S10_MPC_ENABLE=1 ~/DR_competition/.venv/bin/python \
  src/S10_sdk_deploy/interface/robot/simulation/mujoco_simulation_ros2.py

# 模式 A 自动导航（无头可加 S10_USE_VIEWER=0）
S10_MPC_ENABLE=1 S10_MODE=auto_nav ~/DR_competition/.venv/bin/python \
  src/S10_sdk_deploy/interface/robot/simulation/mujoco_simulation_ros2.py
```

> 必须用 `.venv` 的 python（`~/DR_competition/.venv/bin/python`），系统
> python3 缺 JAX/MuJoCo 等依赖。

## 4. 系统架构

```
mujoco_simulation_ros2.py（仿真节点：S10_track.xml 物理 + viewer + 键盘 + 模式 A/B 接线）
  ├─ 感知：mujoco_lidar（120×48 线 @10Hz，前下 45° 安装）
  │     → points_world → local_map.LocalMap（感知-voxel 世界对齐高程瓦片 60×60@0.1m）
  │     → /local_map 话题 + get_local_map()（rollout 注入，零 retrace）
  │     → elevation_lookup.sample_grid / terrain_cost（纯 jnp 查图）
  ├─ 控制：dial-mpc MBDPI（Nsample 2048 / H14 / Ndiffuse 1，plan_once ≈17Hz）
  │     → s10_mpc/mpc_controller.py（CRUISE/STAIR 双模式 + 前馈重注入）
  │     → s10_mpc/auto_nav.py（模式 A：pursuit + 弯道/坡度/台阶限速 + 状态机）
  └─ 配置：doc/s10_mpc_deploy.yaml（hardware_profile 覆盖）
```

### 4.1 感知（perception/）

- `local_map.py`：纯 numpy 世界对齐高程瓦片——3D 体素栅格（0.05m，
  min-z 去噪）→ 2.5D 瓦片（8×8m 锚定、输出 6×6m=(60,60)）→ 重锚定
  （>2m）+ 重叠带继承 + 最近观测优先 + 空洞填补（inpaint 3 格）。
- `elevation_lookup.py`：纯 jnp 固定形状查图，越界/空洞按"未知不惩罚"。
- **运动学地面注入**（`S10_KIN_GROUND=1` 默认开）：轮子法向支撑 >5N 时把
  "轮心−轮半径"并入点云，修复 riser 阴影导致的轮下地图空洞（真机可移植）。
- **LiDAR 安装**（S10.xml `lidar_site`）：`euler="0 0.79 0"`（前上 ~45°）+
  垂直 ±45°（`S10_LIDAR_PHI_DEG=45`），爬坡 pitch 下地面覆盖最优；
  模型保持官方原样（仅保留 LiDAR site 新增，比赛要求）。

### 4.2 控制（s10_mpc/ + dial-mpc）

- **MBDPI 主控制器**（`mpc_controller.py`）：模式化双配置
  - CRUISE：H14（0.28s 视界），腿 σ=0.3，蹲姿正则 15，leg_ext/lockpush 关；
  - STAIR：H20（0.4s 视界），腿 σ=2.0，放开屈膝（w_crouch=0）、
    leg_ext_w=30（抬腿伸展引导）、lockpush_w=8（顶死才锁前轮）、
    r_clear=60（机身逐级抬升）、attdamp=2.0；切模式零 retrace。
  - 恒定项：`terrain_w_ground=300`（轮-地形贴合，台阶处抬轮核心）、
    `w_clear=60`（机身离地净高）。
- **reward 关键项**（`dial_mpc/envs/s10_env.py`）：r_ground / r_upright /
  r_attdamp / 前瞻抬轮（四轮独立窗口 + 陡升梯度门控）/ MARG stumble /
  台阶区 r_leg 放松 / r_ext / r_lockpush / r_clear / r_path*（折线路径）。
- **MPPI 备选**（`mppi_controller.py`）：早期轻量方案，H30/Ns64 ≈ 4.8Hz，
  已由 MBDPI 取代，保留作 Orin 低算力兜底。
- **auto_nav.py**（模式 A）：折线路径滚动采样 + 高程图 z 轨迹；限速 =
  弯道曲率 × 坡度 × 台阶区；`step_zone`（单级横脊，1.5 m/s）与
  `stair_zone`（连续楼梯，2.5 m/s）分开；脱困（5s 无进展前推 +
  航向锁定）；vyaw 变化率限制（0.8）。

## 5. 当前状态（2026-08-06）

- **模式 B（遥控）**：稳定可用。vel_scale=56（≈4.5 m/s）直线
  4.4~4.7 m/s、原地转向 5~6 rad/s、站姿 z≈0.204m（hipx=0.05）；
  yaw 增益速度自适应（低速 50 / 高速 15）+ 轮速斜坡防翘头。
- **模式 A（自动导航）**：wp0→wp6 稳定（34~38s，minisnap 已移除、
  改折线直连）；wp7 前段双 riser 通过率已提升（链 35 3/3 达 wp7）；
  **阻塞点 = wp7 台阶区 4~5 级连续台阶**——链 64 配置单次可爬 3~4 级
  （body z 1.14），中段西漂/侧倾未完全收敛；链 67 基线统计中。
- **频率**：plan_once 56ms ≈ 17Hz（4090）；Orin 档（1024/10/1/CPU）
  估算 2~8Hz 未实测；真机控制不依赖 MPC 频率（200Hz 轮速前馈 + 腿 PD 执行）。
- **预热**：JAX 编译缓存就绪后冷启动 ≈4.3s（比赛部署建议随环境打包
  `~/.cache/s10_dial_mpc`）。

## 6. 待办（按优先级）

1. **wp7 台阶区爬升收敛**：链 64 组合（整体抬升 + r_ext + lockpush 顶死 +
   σ=2.0 + 航向锁定）的稳定性/成功率；左右对称、最后一级腿长极限、侧倾抑制。
2. **全航点 33 点连续跑通**：终点判停；验证陡坡段（wp17→18 +21% 等）与
   高架段（wp28→32 z=3.75）。
3. **真机迁移**：vel_scale 回退 50（真机轮电机 ≈50rad/s ≈ 4.05m/s）、
   IMU 航向闭环（当前用 mujoco 真值）、Orin 实测 plan 耗时、Airy 实机标定
   （仿真 ±45° vs 真机 0~90° 垂直视场）。
4. **A\* 全局规划**（可选，申报自主跟随后降级）：`doc/s10_mpc_deploy.yaml`
   `astar` 段为预留。

## 7. 关键参数速查

### 7.1 硬件档位（`s10_mpc_deploy.yaml` + `HARDWARE_PROFILES`）

| 参数 | desktop_4090 | orin_agx |
|---|---|---|
| Nsample | 2048 | 1024 |
| Hsample | 14（STAIR 20） | 10 |
| Hnode | 4 | 4 |
| Ndiffuse | 1 | 1 |
| dt | 0.02 | 0.025 |
| jax_platform | cuda | cpu |

环境变量硬覆盖：`S10_MPC_NSAMPLE` / `S10_MPC_HSAMPLE` / `S10_MPC_NDIFFUSE` /
`S10_MPC_SOLVER_IT` / `S10_MPC_H_CRUISE`（14）/ `S10_MPC_H_STAIR`（20）。

### 7.2 控制器默认值（生产）

| 参数 | 默认 | 说明 |
|---|---|---|
| vel_scale | 56（yaml）/ 50（env 类） | 轮速上限，56×0.081≈4.54 m/s |
| solver_iterations | 4（yaml） | 高频率版（it=6 卡脊，不用） |
| leg_action_scale | 0.25 | CRUISE 屈膝量；STAIR 放开 |
| kp / kd | 80.0 / 2.0 | 腿 PD |
| kd_wheel / wheel_tau_scale | 1.2 / 3.0 | 轮速伺服 / 力矩尺度 |
| height_weight | 0.1 | 地形跟随高度（实验 3.0 未保留） |
| terrain_w_ground | 300（运行时） | 轮-地形贴合（yaml 120 为旧基线） |
| w_clear | 60（运行时） | 机身离地净高（yaml 无此键） |
| S10_MPC_YAW_FF_GAIN(_HI) | 50 / 15 | 低速/高速转向前馈 |
| S10_MPC_WHEEL_RAMP(_DOWN) | 0.25 / 0.5 | 前进轮速斜坡（防翘头） |
| S10_STAND_HIPX | 0.05 | 胯外翻 |

### 7.3 导航与爬坡（auto_nav / env）

| 环境变量 | 默认 | 说明 |
|---|---|---|
| S10_AUTO_STEP_VX | 1.5 | 单级台阶段限速（横脊，靠动量不行） |
| S10_AUTO_STAIR_VX | 2.5 | 连续楼梯段限速（wp6→7） |
| S10_AUTO_STEP_DIST | 6.0 | 台阶段提前减速距离 (m) |
| S10_STAIR_MODE_DIST | 3.0 | 进入 STAIR 模式的触发距离 |
| S10_AUTO_VYAW_SLEW | 0.8 | vyaw 变化率限制（治打转） |
| S10_AUTO_VMAX / VYAW_MAX | 4.5 / 2.0 | 模式 A 目标上限 |
| S10_STAIR_LEG_SIGMA | 2.0（STAIR）/ 0.3（CRUISE） | 腿维采样噪声 |
| S10_LIFT_MAX / S10_LIFT_STEEP_GATE | 0.15 / 0.6 | 抬轮量 / 陡升梯度门控 |
| S10_LIFT_REAR | 0 | 后轮抬升默认关（前挂后蹬策略） |
| S10_LOCKPUSH_W | 8（STAIR） | 锁轮推身软约束（顶死才激活） |
| S10_LEG_EXT_W | 30（STAIR） | 抬腿伸展引导权重 |
| S10_KIN_GROUND | 1 | 运动学地面注入 |
| S10_LOCAL_INPAINT | 3 | 近场空洞填补格数 |
| S10_TERRAIN_W_* | yaml | slope 2 / rough 1 / step 5 / ground 120（运行 300）等 |

### 7.4 仿真与 LiDAR（mujoco_simulation_ros2.py）

| 环境变量 | 默认 | 说明 |
|---|---|---|
| S10_MODE | remote | `auto_nav` 切换模式 A |
| S10_MPC_ENABLE | 0 | `1` 启用 MPC 控制 |
| S10_MPC_YAML | `doc/s10_mpc_deploy.yaml` | 部署配置 |
| S10_MPC_PLAN_INTERVAL | 50 | MBDPI 规划间隔（仿真步） |
| S10_USE_VIEWER | 1 | 无头设 0 |
| S10_MUJOCO_SCENE | track | 场景（S10_track.xml） |
| S10_MUJOCO_XML | （可选） | 自定义 MJCF（加传感器） |
| S10_LIDAR_BACKEND | cpu | WSL 下勿用 taichi（core dump） |
| S10_LIDAR_FREQ / PHI_N / THETA_N | 10 / 48 / 120 | LiDAR 频率与线数 |
| S10_LIDAR_PHI_DEG | 45 | 垂直半 FOV（配合 site euler 0.79） |
| S10_LIDAR_FOV_H_DEG | 90 | 水平半角（180=360°） |
| S10_SELF_TEST_SECONDS | 0 | >0 无头自检后自动退出 |

## 8. 文档与测试入口

- **本文档**：`doc/0806.md`（唯一工程文档）
- **参数**：`doc/s10_mpc_deploy.yaml`
- **官方材料**：`doc/比赛规则_赛道四_具身未来.md`、`赛道四_具身未来.pdf`、
  `Airy雷达用户手册.pdf`、`Airy User Guide.pdf`、`hardware spec.pdf`
- **关键代码**：
  - 仿真节点：`src/S10_sdk_deploy/interface/robot/simulation/mujoco_simulation_ros2.py`
  - 感知：`src/S10_sdk_deploy/perception/`（local_map / elevation_lookup / minisnap）
  - 控制器：`src/S10_sdk_deploy/s10_mpc/`（mpc_controller / auto_nav / mppi_controller）
  - 环境：`dial-mpc/dial_mpc/envs/s10_env.py`、`core/dial_core.py`
  - 模型：`src/S10_sdk_deploy/S10_description/s10_mjcf/mjcf/s10_mpc.xml`（rollout）、
    `s10_track_view.xml`（仿真）、`S10.xml`（官方模型）
- **历史实验**：见归档 `_archive_20260806/0804.md` 与
  `_archive_20260806/tmp_competition_experiments/`（链 1~67 全记录）。

