# RL-Stair 验收报告（定稿）

## 1. 官方比赛环境验收（最终口径）
- 环境：官方 S10_track.xml + 官方环境（numpy 1.26.4 / mujoco 3.11，venv comp_env）+ RL-Stair 部署控制器。
- 结果（30 seed，RL 交接点 spawn_y=34.0、进场 vx=1.5）：**reach=29/30（96.7%）达标**。
- wp6→7 平均完成时间 **7.96s**（7.55–8.66s）。
- 轨迹图：`doc/figures/wp67_traj_officialenv_96pct.png`（xy，颜色=速度）。

## 2. sim2sim 精确几何 harness
- 验收口径：爬完 6 级 + 后腿登顶后 ~1m 直立（wp7 交接点）。
- 最佳模型（model_000400_75pct_keep → deploy/policy.pt）：1.5 m/s 进场 **17/20（85%）**；wp6→7 平均 4.65s。

## 3. 模式切换与感知（高程图）
- CRUISE⇄STAIR 高程图判定已实现并单测通过（合成高程图 4 例全过）；接近减速平滑、越过自动交还。
- riser 表由 lidar 在线生成并注入策略观测（terrain_ctx）。

## 4. 部署控制器端到端
- 训练 → 导出 → 部署控制器全链闭环验证通过：部署端与评估端行为逐位一致（20 seed 17/20）。

## 5. 关键文件
- 训练/策略：`rl_stair/`（楼梯专用：去横脊、T6 纯楼梯交接、best 跟踪、DR、速度奖励）
- 部署：`rl_stair/deploy/{policy.pt, obs_np.py, rlstair_ctrl.py, elev_tile.py}`
- 模式切换：`src/S10_sdk_deploy/s10_mpc/auto_nav.py`；接线：主链路 S10_RL_ELEV=1