# Stair 技能机制分析（2026-08-11，v750）

## 当前状态

- **巡航（wp0→6）**：稳定通过 31.2s，各段均 <10s（v749 基线）。
- **Stair（wp6→7，六级台阶 0.063+5×0.125m）**：**未突破第一级**，三种执行层方案（WBC/FootPlace/CarVMC）均卡死或侧翻（历史 84+ 组 WBC 实验）。

## Stair 卡死根因链（v771~v792 共 21 组实验确认）

### 模式 A：CPG 抬轮 + WBC（dual 模式）

1. CPG 前轮抬轮（sl 0.5~0.9），前轮悬空（fn=0）。
2. WBC 按 `sl>0.3` 屏蔽抬轮腿 → 前轮不参与 wrench 求解 → 只有后轮支撑 → **单点支撑**（fn=[0,0,-25,0]）。
3. 后轮单点推力不足 → 车不前进 → CPG 相位冻结 → 前轮持续悬空卡死。

关键数据（v790）：卡死时前轮 wz=0.66（高于第一级台面+r=0.613），水平位置 y=37.65（棱前 0.29m），pitch≈0，后轮单点 -21N。

### 模式 B：纯滚动 + WBC（NO_LIFT）

1. 前轮贴地滚向棱边，但 v746 运动学地面（`terr=wheel_xyz-0.081`）是"自证"的：轮子悬空时 terr 跟随轮位，阻抗永远≈0，不纠正悬空。
2. WBC 给前轮"支撑力"（想前倾）但前轮无接触面 → 力推空气 → body 被推高 → **body 后倾**（pitch -0.2）→ 前轮更悬空。
3. 前轮距台面仅 0.003~0.008m 不接触（mujoco 接触间隙），压轮补偿不足。

关键数据（v785~788）：前轮 wz 0.61 vs 台面+r 0.613，差 0.003 不接触；pitch 持续后倾 -0.18~-0.27；fn 单轮或全 0。

### 模式 C：FootPlace IK 放轮（dual2 模式）

- CPG 抬轮 + IK 把轮放到台面+r → 放太高导致车身弹起翻转（roll 1.34→-2.75）。
- 历史 v704 同模式（姿态刚度×5 仍翻），确认是"轮目标-身体姿态正反馈"。

## 根因总结

1. **悬空抬轮中断推力**：CPG 抬轮时前轮离地（fn=0），轮速归零（v737），只剩后轮推 → 单点支撑死锁。
2. **WBC 支撑腿选择不基于物理接触**：按 sl 屏蔽而非接触检测，悬空前轮仍参与 wrench → 不物理的拉力/推力。
3. **运动学地面自证**：terr 跟随轮位，悬空轮不被纠正。
4. **缺少关键姿态**：前轮在台面时 body 必须前倾（前低后高），当前 WBC 无法产生前倾力矩（前轮无接触）。

## 文献方案（USC 2109.09934，真机 0.36m 台阶成功）

- **不悬空抬轮**：轮子持续驱动 τ=R·F_t，姿态前倾 + 腿伸长让轮心沿棱边滚动爬升，接触连续。
- **姿态优化**：解"前轮在台面/后轮在地面"的关键姿态（body pitch + CoM + 关节角），两个关键姿态线性插值。
- **QP 平衡控制**：腿力 QP（摩擦锥约束，力只能推），轮子单独滚动控制。

## 下一步建议

1. **实现 USC 式姿态插值**：楼梯区按"前轮距棱距离"插值 body pitch 和前腿伸长量（替代启发式 pitch），保证前轮接触台面时 body 前倾。
2. **WBC 支撑腿按物理接触选择**：用轮心高度 vs 地形目标（<0.01m）判接触，悬空轮不给 wrench 力。
3. **轮子持续驱动**：取消抬轮时轮速归零（v737），改"腿伸长辅助滚动"，保持 4 轮接触。
4. 若短期无法突破：先跳过 wp6→7，并行推进 wp7→33（急弯已部分验证到 wp15）。

## 更新（2026-08-11 晚，v755→v772 文献方案落地批次）

### 已落地的文献修复（全部提交）
1. **显式轮心 z 参考**（v755）：stair 区 WBC 地形用 `stair_wheel_ref`（riser 表解析 ramp/step）替代"轮位-半径"自证地面；左右轮参考对称化（防 yaw 漂移→ramp 相位差→roll，实测 wz 0.78/0.95 侧翻）。
2. **楼梯区轮矩解锁**（v755b）：横脊表含楼梯 riser，`_ridge_d<0.8` 时 μN·r 钳制把轮矩压到 1.7Nm 无推力（卡死根因之一）；stair 区跳过钳制，轮矩恢复 13.5Nm。
3. **移除棱前 0.30m 提前抬 terr 覆盖**（v755b）：提前抬高把前轮卸载 fn=0；由 stair_wheel_ref ramp 显式生成。
4. **踩实释放**（v755c/v757/v772）：接触反馈调制 CPG 相位（Kimura/ETH 腿效用同源）——前轮过棱且轮高达台面即连续释放抬升；参考用"刚越过的棱"而非"最近棱"。
5. **body z 控制恢复**（v755e）：楼梯区可开 S10_VMC_WBC_Z_KP=800（原 v605 取消 z 抬升→抬腿反作用把 body 顶起后轮悬空）。
6. **单侧垂直阻抗 + 下压余量**（v763/v764）：双侧弹簧把轮"吸"离台面悬空（fn=0）；改只下压不吸抬 + 下压 6mm 压实（mujoco 接触间隙 3-8mm）。
7. **楼梯 pitch 常开 + 力矩上限 25→80 + 关加速俯冲前馈**（v765/v770）：原 pitch 只在 step_lift>0.02 时生效（NO_LIFT 掉回巡航公式）；且 T_pitch 被 25Nm 钳制（抬轮反作用饱和，pitch 实测 -0.3 低头）；放开后 pitch 收敛到 0±0.15。
8. **楼梯区保留抬轮腿进 wrench**（v767）：sl>0.3 屏蔽后 pitch/z 控制对前腿无权威（可选项 S10_VMC_WBC_KEEP_LIFT=1）。
9. **小台阶（≤轮半径 0.081）不抬轮**（v769）：首级 0.063m 纯滚动可过，提前抬轮反而失牵引。

### 当前状态（v772）
- 首级 0.063m 台阶：可滚过（v769 后不再抬轮）。
- riser2（0.125m）：前轮可达棱角高度 0.74-0.78（目标 0.747+r），pitch≈0，**但卡在棱角**：轮心 0.74-0.78 时轮底 0.66-0.70 与棱顶 0.666 平齐，悬空无接触（fn=0）→ 无牵引 → 后轮推不动 → 滑回。反复在 y=37.9-38.1 振荡。
- 尝试过：NO_LIFT 纯滚动（0.063 可滚、0.125 刚体轮爬不上垂直面，确认不可行）、慢速 0.9（反而卡棱前）、抬轮上限 0.55/0.75/0.9（0.75 最优，0.9 过抬悬空）、RAMP_A 0.05/0.14/0.30（无本质改善）。

### 根因链（v755→v772 实证）
1. 0.42m 台面间距 + 0.125m 台阶高：轮子需在 0.42m 行程内上升 0.125m（抬）再下落 0.125m（落）。1.8m/s 时落地窗口仅 0.12s，自由落体不够 → 必须主动下压（已做）但轮子在棱角处"骑"住。
2. 抬轮（关节卷曲）反作用把 body 压低头（pitch -0.3），需大姿态力矩（已放开 80Nm）才收敛。
3. 轮在棱角高度悬空无接触 → 无 fn → WBC 轮矩上限依赖 fn 的路径失效/无牵引 → 无法前进 → 释放不触发 → 死循环。

### 下一步候选（未验证）
A. **棱角高度精确控制**：抬轮目标 = 棱顶+r+0.02（0.757）用闭环轮心 z 控制（而非开环关节卷曲），过棱后释放 + 下压落地——需要"轮心 z 目标"直接进 WBC 阻抗（现在经 terr 间接）。
B. **前抬后推相位**：前轮在棱角时后轮必须贴地强推（轮矩满 13.5Nm + 后腿伸长压地），当前后轮 fn 常为 0（body 悬）。
C. **USC 姿态插值重做**：v752 stair_pose（前腿伸长后腿收缩）与 WBC 二选一主导，避免打架。
D. 若继续卡：先跳过 wp6→7，验证 wp7→33 全赛段（wp9 急弯/wp11 墙需单独调），再回头攻坚 stair。
