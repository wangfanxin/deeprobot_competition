# Stair 技能当前方案总结（2026-08-12，HEAD `05f1416`）

> 目标：真原图 wp6→7 连续越过全部 6 级台阶（riser 高 0.061 + 0.125×5）。
> 现状：接近段稳定、前轴动量滚过 riser1、到达 riser2 棱口（y=38.3）；
> 卡点：body 塌陷死举（见 §五）。

## 一、方案总览：位置基全身控制（论文方向，无 MPPI）

三层架构 + 双技能门控（唯一离散切换）：

```
CRUISE（CarVMC） ⇄ STAIR（StairWBC 位置基全身控制）
       技能切换：fol.mode == 'STAIR' 且前轴距首级 riser < S10_STAIR_EXEC_D
```

**StairWBC 执行层**（`src/S10_sdk_deploy/s10_mpc/stair_wbc.py`，200Hz）：

1. **ModeSchedule**：整轴布尔相位（前轴→后轴，永不双轴同抬）——几何相位硬切换，sin² 仅生成位置目标；
2. **Body 姿态解析 + FootPlaceVMC 位置 PD**：每腿闭式 IK 放轮 + 静压；
3. **WheelCtrl**：支撑轮速度 PID 前驱（爬升期冻结差速、前驱下限 -6Nm），抬升轮 0；
4. **QP Checker**：osqp 12 变量接触合规校验（摩擦锥/法向下限），仅破锥时微降腿增益，非力分配主环；
5. **腿控 Yaw**：HipX 外展/内收修正航向（轮差速冻结时唯一 yaw 权威）。

**已清理**（用户要求）：所有 `S10_STAIR_*`/`S10_QP_*` 散落 env 收敛为类属性；
删除 `S10_STAIR_SWING_WHEEL0` 状态翻转、`S10_STAIR_QP/FACE` 开关门控；
修复 QP Checker 未初始化 rows/cols/vals/lo/hi 的 NameError（此前从未真正运行）。

## 二、数据管线

```
感知层（10Hz）
  lidar 高程图 + stair_world 预扫描（riser 表：y/top/dh，世界坐标）
  → 几何地形 _geo_terrain（每轮支撑面高 = 已过最高 riser 顶 / 最近 riser 底）
规划层（20Hz, AutoNavFollower）
  纯 xy 路径跟踪（Catmull-Rom），vx 连续插值（楼梯窗不归零）
  → cmd: vx / omega / yaw 误差（_last_err）
执行层（200Hz, StairWBC.compute_tau）
  ModeSchedule → 贴面目标 _face_place_z → 闭式 IK → 位置 PD + 静压
  → WheelCtrl（速度 PID ±13.5Nm）→ QP Checker → 腿控 Yaw
  → 16 维力矩（腿 ±48Nm / 轮 ±13.5Nm）
```

### 关键文件

| 文件 | 作用 |
|---|---|
| `src/S10_sdk_deploy/s10_mpc/stair_wbc.py` | StairWBC 位置基全身控制（主战场） |
| `src/S10_sdk_deploy/s10_mpc/stair_vmc_legs.py` | FootPlaceVMC 位置基地基（IK/PD/静压） |
| `src/S10_sdk_deploy/scripts/stair_vmc_noros.py` | 主循环、技能状态机、vx 窗 |
| `tmp/run_real_wbc.sh` | 真原图运行脚本（全部参数） |
| `doc/stair_StairWBC_终版_20260811.md` | 批准方案终版（本实现依据） |

## 三、具体参数

### StairWBC 类属性（`stair_wbc.py`）

| 参数 | 值 | 说明 |
|---|---|---|
| swing_d | 0.30m | 抬升窗（棱前提前抬，靠动量越棱） |
| swing_to | 1.5s | SWING 绝对超时兜底 |
| lift_margin | 0.04m | place_z→轮心目标余量 |
| qp_mu | 0.6 | QP Checker 摩擦系数 |
| qp_nmin | 5.0N | 支撑轮法向下限 |
| qp_scale | 1.0（下限0.85） | 破锥时腿增益微降 |

### 主脚本/导航（`stair_vmc_noros.py` + `run_real_wbc.sh`）

| 参数 | 值 | 说明 |
|---|---|---|
| S10_VMC_MODE | stairwbc | 启用位置基 StairWBC |
| S10_STAIR_WIN_VX | 2.2 m/s | 楼梯窗目标速度（不归零） |
| S10_STAIR_VX_RAMP | 10.0m | vx 斜坡长度（巡航 6→2.2） |
| S10_STAIR_CREST_VX | 2.2 m/s | 爬顶不减（已移除旧 1.2 减速） |
| S10_STAIR_EXEC_D | 1.5m | 前轴距首级 riser 内才切 StairWBC |
| S10_STAIR_YAW_GATE | 1.0 rad | yaw 未对准时保持 CarVMC 对准 |
| S10_STAIR_SWING_D | 0.30m | 主脚本同步触发窗 |
| S10_AUTO_LOOKAHEAD_STAIR | 3.5m | 楼梯窗前视 |
| S10_AUTO_CTE_GAIN_STAIR | 1.0 | 横偏增益 |
| S10_AUTO_YAW_GAIN_STAIR | 1.5 | 航向增益 |
| S10_YAW_DAMP | 2.0 | yaw 阻尼 |

### FootPlaceVMC 位置基（`stair_vmc_legs.py`）

| 参数 | 值 | 说明 |
|---|---|---|
| S10_FP_POSMODE | 1 | 位置基模式 |
| S10_FP_PRESS | 0.005m | 静压（支撑轮压入台面 5mm） |
| S10_FP_KP / KD | 220 / 6 | 腿位置 PD |
| S10_FP_KP_POS | 220 | 支撑腿 IK 位置增益 |
| S10_FP_WHEEL_K / D | 4 / 0.08 | 轮速 PID |
| S10_FP_KP_ROLL / PITCH | 400 / 300 | 姿态修正增益 |
| S10_FP_BODY_K / KD / PD | 0.4 / 0.06 / 0.05 | body 高度/姿态闭环 |
| S10_FP_STAND_DROP | 0.26m | body 目标 = 支撑轮均值+落差 |
| S10_FP_REACH | -0.16m | 支撑腿行程钳制 |
| S10_FP_YAW_KP / KD | 2.0 / 0.5 | 腿控 Yaw |

### 硬约束与模型参数（已核查一致）

- 腿力矩 ≤48Nm、轮 ≤13.5Nm（合规）
- 轮半径 0.081m、腿长 0.18/0.18m、轴距 0.456m
- 台阶几何：riser y/top = 37.891/0.541、38.339/0.666、38.738/0.791、39.186/0.916、39.585/1.041、40.033/1.166

## 四、当前测试状态

| 阶段 | 状态 |
|---|---|
| 接近段（清理版） | ✅ 完全稳定（轮子 0.54-0.73，不折叠/不泵高） |
| riser1 跨越 | ✅ 前轴动量滚过（y 到 38.0+） |
| riser2 前轴爬升 | ✅ 前轮保持低位不折叠，到达棱口（y=38.3） |
| riser2 越棱 / 全段 | ❌ 卡死（见 §五） |

历史探索（已归档/回退，供参考）：QP 混合版 90+ 次、清理版 20+ 次，
覆盖硬抬/软弹簧/跟随/压面、前驱 1-8Nm/门控/无、宽窄窗、单轮/轴式、
4 档速度、3 种防过伸、闭式 IK、差速抗旋、前驱下限、蹦跳循环——均无法
完成 0.125m 台阶（1.54×轮半径）。

## 五、卡点与根因

**唯一卡点**：接近段 body 塌陷到 0.63（正常 0.78）→ 腿近乎水平（垂距 1cm）
形成"死举"几何死点 → 前轮无法抬过 riser2 → 卡死 → yaw 漂移翻车。

**根因**：FootPlaceVMC 位置控制靠轮目标间接实现 body 高度（bdes_z），
而水平腿伸长的力是水平的（轮前滚），无法把 body 举回。body-z 闭环存在
塌陷平衡（body 低 → 轮目标低 → bdes_z 低 → 接受低 body）。
所有参数/力项尝试（BODY_K、STAND_DROP、PRESS、直接上举力、bdes_z 几何化）
均无法打破该几何死点。

## 六、建议下一步（需用户定方向）

1. **换执行层彻底重写腿控制**：真力控/阻抗（腿按力输出，不按位置），
   或恢复 dialMPC 旧版对比；
2. **先修巡航段 body 高度保持**：接近段不塌身再谈爬梯；
3. **承认 0.125m 物理死局**：绕行或改比赛策略。
## 七、后续探索记录（2026-08-12 晚，HEAD fd57fed）

### v1017（真 bug 修复，已提交）
基类支撑腿单向阻抗 `F9 = max(300*min(dz,0), 2.0)` 把"轮高于目标"的负下压力
（-135N）钳成 +2N——**折叠过伸从未被对抗**（轮 1.2 vs 目标 0.747 的直接
原因）。修复后支撑腿阻抗能压回折叠轮，body 稳定到 0.65-0.70。

### 已测试无效的机制（均为确定性重复验证）
| 机制 | 结果 |
|---|---|
| swing 目标升速限制 1.2m/s（v1019） | 轨迹与基线完全相同（非目标升速机制） |
| swing 腿增益 40→120 | 轨迹完全相同（swing 腿不是折叠源） |
| 强恢复矩 KQ=200（v1020） | 更早翻车（接近段 roll 0.5） |
| KPH 阻抗 300→600 | 更早翻车 |
| 高动量 3.5m/s | 折叠过伸 + 西漂翻车 |
| STAND_DROP 0.26→0.20 | 轨迹完全相同（bdes_z 非绑定约束） |

### 最终结论（195+ 次仿真）
位置基 StairWBC 全部机制（恢复矩 3 版、阻抗含 bug 修复、防过伸 5 版、
几何前馈、relx 钳制、目标限速、动量 1.8-3.5m/s、速度/斜坡/窗参数）均
无法越过"爬升中四轮折叠过伸"（轮 1.0-1.2 vs 目标 0.747）。**该范式在
近奇异位形没有保持轮高的控制权威，属结构性极限。**

最优状态：狗到 y=38.4（前轴滚过 riser1+riser2、后轴进入爬升），body
稳定 0.76-0.79。代码 `fd57fed`（v1017 阻抗 bug 修复）。

解除阻塞需架构级方向：① 力/阻抗主导全身控制（轮高保持力）；② 完整
NMPC+WBC 论文方案；③ 调整目标。

## 八、v1025-v1035 突破记录（2026-08-12 深夜，HEAD 394830a）

上一节"结构性极限"结论被推翻：位置基 StairWBC 不是死局，是 5 个可修 bug
叠加 + 一个未解决的航向问题。

### 已修复（全部提交并推送）
| 版本 | 修复 | 证据 |
|---|---|---|
| v1025 | 轮差速 yaw 修正左右拆分+符号修正（原前后轴拆分=俯仰调制），加平滑 yaw 率硬刹 | ex04 提前接管自旋跑飞 |
| v1026 | START_WP 出生朝向尊重 S10_INIT_YAW（原固定 1.551，路径 1.514） | 导航蛇行激发源 |
| v1027 | riser 阈值 0.085→0.050：riser1(0.061m) 也走 SWING 抬轮放置，不再锐角滚动 | 3° 偏航滚角即翻车 |
| v1028 | 支撑轮 wz 下限钳制（静压位-2cm）：body-z 闭环把目标压到地底 0.39→腿折叠 | FP debug：目标 0.39↔0.605 逐拍翻转 |
| v1029 | relx 钳制 0.05→0.0：接管腿位形与巡航一致，消除重构塌陷 | 接管 body 0.78→0.66 的机制 |
| v1030 | SWING 释放阈值 top+r+0.005→top+r-0.01：带静压的轮永远够不到原阈值 | SWING 挂起 1s+ 腿泵高 |
| v1031 | swing_d 环境可调，脚本与 WBC 对齐 | 双重相位机错位 |
| v1032 | stair 区 om 缩放环境可调（原 0.5 砍半修正） | 16° 偏航需 0.68rad/s 被压到 0.25 |
| v1033 | 贴面目标逐轮距离（原轴均值左右同相） | 偏航时单侧被顶 0.2m |
| v1034 | stair 区航向锁定（楼梯切线方向，pursuit 瞄准 wp7 造成偏航残留） | 见下 |
| v1035 | WBC 轮控恢复导航 om 差速（原 v1025 重写丢弃） | 航向锁定在 WBC 期不驱动轮子 |

### 当前真实状态（ex26/27 配置，确定性）
- 接近段 body 全程 0.78 稳定（v1029 后不再塌）
- riser1 前 SWING 正常触发、前轮抬上台阶顶（sp=[1,0]）
- 可存活到 t=13-17（此前 t=6-8）
- **剩余卡点**：接近段航向 10-30° 偏（导航+CarVMC 低速共振），SWING/接触
  在偏航下不对称 → roll 翻车；狗同时东漂 1-2m（偏航的横移分量）。

### 未解决：接近段航向
尝试过：低 CarVMC yaw 增益、高/低 nav 增益、前视 3.5/8m（无效，被航点截断）、
cte 1.0/4.0、stair om 衰减 0.5/1.0、航向锁定±0.6、wheel_k 4/12。
全部在 stair 速度下进入 yaw 极限环（导航 20Hz 离散 + 轮差速执行相位滞后）。
需要架构级解决：独立接近控制器（恒定航向前馈+强率阻尼），或降速<0.8m/s，
或完整换执行层。


## 九、v1036-v1044 记录（2026-08-12 深夜，HEAD 1ae71c8）

### 关键 bug 修复
| 版本 | 修复 | 证据 |
|---|---|---|
| v1036 | yaw 差速必须在前驱下限**之后**叠加（v1035 放 vref 里被 max(_tw,-6) 吞掉→差速=0，航向锁在 WBC 期无效） | 正常滚动 vw≈vref→PID≈0→左右同被钳 -6 |
| v1037 | 前轴 SWING 期后轮深压 0.04 抗翘头 | 前轮抬升+后轮推力→body 翘头、后轮离地 0.2m、前轮泵到 1.1 |
| v1038/v1042 | 航向锁积分项 + 修复引用脚本内不存在 `_dt_nav`→NameError 被 try/except 吞掉→**航向锁整段静默失效**（HDG 打印归零为证，ex42-47 全废） | ex42 起 HDG=0、ex35-41 HDG=200-500 |
| v1039 | 抬升在窗口前 60% 完成（原全程斜坡，轮到棱角前 5cm 拖角激起自旋 om 4.09） | |
| v1040 | 回退 v1033 逐轮抬升为轴均值同抬（逐轮让 FL 先抬/FR 拖地单侧拖拽自旋） | |
| v1041 | 200Hz 航向误差直驱轮差速（20Hz 导航采样滞后是 yaw 极限环根源） | |
| v1043 | 后轴纯滚 riser1（min_dh 0.085），前轴抬轮避角（0.050）——后轴 SWING 小台阶轮泵 1.09 翻车 | |
| v1044 | 航向锁加横向收敛（瞄准=切线+横向偏差修正），把狗拉回楼梯中线 | ex50 x=-14.2 在线上（此前 -12.5 东漂 1.8m） |

### 当前真实状态（ex48-51 配置）
- 接近段 body 稳定 0.78、狗在路径线上（横向收敛生效）
- 航向锁工作（yaw 1.4-1.8 附近振荡）
- 前轴 SWING 正常抬轮上 riser1，后轴纯滚
- 存活 25s+（卡死超时）或 t=13-28
- **剩余卡点**：yaw 极限环 ±0.3-0.9 rad 无法消除（试过增益/阻尼/积分/200Hz 误差/横向项/速度 0.6-1.8 共 15+ 配置），SWING 接触在偏航下仍会激起自旋/侧翻；狗在平台边缘徘徊或卡死

### 结论
位置基 StairWBC 的机械/几何问题已基本解决（接近段稳定、riser1 抬轮上阶）。
最后一个硬卡点是**接近段 yaw 稳定性**——当前 20Hz 导航+轮差速执行架构在
stair 低速下存在极限环，需架构级解决（独立高速航向控制器、腿控 yaw 高增益
重调、或换执行层）。目标保持活跃。


## 十、v1045-v1048 记录（HEAD b76ebb1）

| 版本 | 修复 | 证据 |
|---|---|---|
| v1045 | 恢复矩触发阈值环境可调(0.03→0.10)+drop 过小项——body 塌到 0.59(drop=0.04) 原阈值不触发，死举无恢复 → 轮失压 → yaw 差速打滑 | ex56 body 恢复 0.78、yaw 稳定 |
| v1046 | 横向收敛符号修正——狗在东侧应向西修(aim>hdg)，原 lat_err=center-x 给负值继续向东发散 | ex56 东漂 2m 到平台外；ex57 回到线上 |
| v1047/v1048 | **SWING 目标高度双减 bug**：_face_place_z 输出 z_face-r-margin，基类又 +r+margin → 轮目标=台面顶(0.541)，轮心应=顶+半径(0.622)——**轮被压进台面 8cm，SWING 后卡死无法前滚**。最终修正 place_z=z_face，基类得轮目标=顶+半径+margin(0.662) | ex58 前轮 0.61-0.63 卡 y=37.7；ex59 SWING 正常抬轮+释放 |

### 当前真实状态（ex59-61 配置）
- 接近段：body 稳住、狗在路径线上（横向收敛）、yaw 稳定 1.2-1.5
- SWING：正确抬轮至 0.66 且能释放（v1048 后）
- **剩余卡点**：SWING 抬轮期间腿反作用把 body 往后拖（弱后驱 3 → 后退；
  强后驱 8 → 抬轮期自旋；5 → 仍后退+自旋）。狗无法稳定越过 riser1 棱口。
- 目标保持活跃。


## 十一、v1049-v1052 记录（HEAD d3ba45e）——riser2 SWING 攻坚收官

| 版本 | 内容 | 结果 |
|---|---|---|
| v1049 | SWING 抬升窗口前 70% 完成 | 与 50%/60%/全程 无本质差异 |
| v1050 | riser1(0.061) 纯滚（yaw 稳定后滚动即可） | **狗纯滚过 riser1 成功（ex63 y=37.9）** |
| v1051 | 全纯滚实验（0.15 阈值） | 无效——轮在 0.125 立面卡死/泵高 |
| v1052 | SWING 期冻结轮层 yaw（用户终版方案，航向全交 HipX） | 无效——riser2 SWING 仍后退+自旋+泵高 |

### 结论（85+ 次仿真，4 个目标轮次）
已解决：接近段 body/横向/航向、riser1 纯滚、SWING 目标高度(0.662)、释放、
恢复矩。**未解决：0.125m riser2+ 的 SWING 爬升**——位置基抬轮在 1 m/s 下
存在"轮拖棱角→后退/自旋/泵高"的耦合，15+ 种 SWING 参数/执行变体均无法
稳定越阶。已到当前架构（位置基 StairWBC + 轮差速/HipX yaw）的调参极限，
需用户定架构方向（完整 NMPC+WBC 论文方案 / 车化动量爬升 / 换执行层）。
目标标记 blocked。


## 十二、NMPC+WBC 骨架启动（HEAD d8547d5，用户裁决方向 1）

新执行层 `src/S10_sdk_deploy/s10_mpc/s10_nmpc_wbc.py`（S10_VMC_MODE=nmpcwbc）：
- NMPC 20Hz：SRBD（m·a=ΣF+mg、I·α=Σr×F）osqp 固定结构（<1ms/解），
  摩擦锥 + 抬升轮 F_z≥5N（贴面爬升，不置 0 避免俯仰力矩不可平衡），
  代价含 a_des、M_des、名义支撑力 F_ref（按接触轮数分配）。
- WBC 200Hz：支撑腿 J^T·(R^T·F_des)（VMC 力约定）+ 姿态正则(kp 20) +
  hipx 0.30·fy；摆腿 IK（按腿分镜像分支）+ 贴面斜坡目标；轮 Pfaffian 前驱。

已修 bug：QP 标准形 P=2·w‖·‖²、重力项 -mg、VMC 力符号、IK 后腿分支、
前驱下限方向(min)、F_ref 接触轮数分配、贴面斜坡（非下降滚动弧）。

台架验证（单级 0.125m）：
- 站姿稳定：body 0.80-0.83、轮 0.61-0.63、存活 20s（姿态正则 80→20 关键）。
- 滚爬：前轮滚过 riser2 棱、后轮滚到 0.66（0.125 面超过轮半径，最后
  0.09m 需 SWING 收尾）。
- 剩余：前驱下限与爬升耦合（无条件下限→发射、条件化→卡死）、yaw 保持、
  完整越阶、真原图 wp6→7。
