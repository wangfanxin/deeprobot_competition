# S10 组合技能交付总结（2026-08-16）

> 目标口径：0) stair 复训 95%；1.1/2) 两个硬切换接管；3/3.1) MPPI 避障+扫墙判别；4) 分段 ref_v。
> 信息权限：比赛原图测试，仅 wp 绝对坐标 + 当前坐标（lidar 感知）。

## 1. 交付状态总览

| # | 目标 | 状态 | 验证证据 |
|---|---|---|---|
| 0 | stair 95%（0-2m/s + 0-3m） | ✅ 完成 | deploy/policy.pt 分布扫描 **190/200=95.0%**（sim2sim_exact --jit） |
| 1.1 | cruise→stair 接管1 | ✅ 完成 | S10_TK1=1，wp5→9 全通（yaw84°/vx2.27/leg_err0.12） |
| 2 | stair→cruise 接管2 | ✅ 完成 | S10_TK2=1，wp5→9 全通（yaw err 1.24→0.55） |
| 3 | MPPI 避障 | 🟡 实现+单测 | costmap2d ESDF + BodyMPPI 软势；真实 costmap 验证；行为级坡/墙判别待专项 |
| 3.1 | 扫墙+判别 | ✅ 完成 | 墙通道(S10_LIDAR_WALL) + 高度判别（墙高 vs 台阶每级≤0.3），实测东行段墙165格/台阶riser排除 |
| 4 | 分段 ref_v | ✅ 完成 | S10_REFV_SEG_LIST="4:2.0"，seg4 vlim 2.0/seg0 6.0 |

## 2. 新增能力与门控（全部默认关，不影响基线）

| 能力 | 门控 env | 说明 |
|---|---|---|
| 墙通道（扫墙） | `S10_LIDAR_WALL=1` | lidar 近水平射线批 → wall_h/wall_valid |
| MPPI 避障 | `S10_MPPI_OBSTACLE=1`（仅 CRUISE） | costmap2d 截断 ESDF + 软势 rho=(d_safe-d)^2/d_safe^2 |
| 接管模式1 | `S10_TK1=1` | 过 wp6 后 lidar 判楼梯角度对准+减速（死区 S10_TK1_YAW_DB） |
| 接管模式2 | `S10_TK2=1` | 交回后 yaw 对齐 path（死区 S10_TK2_YAW_DB） |
| 分段 ref_v | `S10_REFV_SEG_LIST="i:v,..."` | 只降指定航点段 vlim |

## 3. 关键代码

- `src/S10_sdk_deploy/s10_mpc/lidar_terrain_v2.py`（墙通道）
- `src/S10_sdk_deploy/s10_mpc/costmap2d.py`（2D ESDF + 墙/台阶判别，新增）
- `src/S10_sdk_deploy/s10_mpc/body_mppi.py`（障碍软势代价）
- `src/S10_sdk_deploy/s10_mpc/auto_nav.py`（分段 ref_v）
- `src/S10_sdk_deploy/scripts/cruise_vmc_noros.py`（TK1/TK2/避障 wiring）
- `rl_stair/sim2sim_exact.py`（--jit 支持，量化 deploy 策略）

## 4. 剩余专项（已定位，非核心硬性）

1. **项 3 行为级**：起步缓坡边缘被墙通道误判为障碍（诊断：OBST=on 时起点 x[-8,7.7] y[4,16] 累积 400-900 格）→ 需"坡 vs 墙连续性"判别（沿 path 高度连续=坡，横向突变=墙）。
2. **wp4→5 硬边界**：完整失败链已定位——SIGN=-1 U-turn / SIGN=+1 VX1.5 卡台阶 / VX2.5 越台阶东偏2m未注册 wp5 / 判点放宽无效 / 横向拉回西侧符号反 → 需"航向感知"的横向纠偏。

## 5. git 记录（本轮关键提交）

- 墙通道、costmap2d+BodyMPPI 软势、TK1/TK2、分段ref_v、95%分布验收、起步门架修复、wp4→5 诊断，均已提交并 push origin/main。

*状态：核心目标(0/1.1/2/3.1/4)完成；项3行为级 + wp4→5 为下一步独立专项。*